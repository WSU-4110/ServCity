function DisplayRecommendedAlignment(service) {
  if (service === "Alignment") {
    message = "Displaying Recommended Alignment service";
  }
  return message;
}

module.exports = DisplayRecommendedAlignment;
